export interface TD { zip: string, latitude: number, longitude: number, city: string, state: string }
export interface IProps { latitude: number, longitude: number, address: string, seller: string, owner: string, state: string, city: string, price: number, description: string, image: string}